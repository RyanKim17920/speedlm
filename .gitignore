# Python
__pycache__
*.py[cod]
build/
dist/
*.egg-info/
.eggs/

# Virtual environments
.venv/
venv/
env/

# Tool caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Environment files
.env
.env.*

# Model artifacts
*.safetensors
*.pt
*.bin
*.gguf
*.onnx

# Local artifact directories (root-anchored: src/speedlm/traces/ is source, not an artifact)
/runs/
/traces/
/profiles/
/artifacts/
.speedlm/

# OS / editor
.DS_Store
.idea/
.vscode/

# Hypothesis
.hypothesis/

# Agent tooling
.serena/
.claude/settings.local.json
# Sentrux writes baseline.json here for `sentrux gate`. Machine-local: it is a
# snapshot of one checkout's metrics, so sharing it would make the regression
# gate compare against whoever committed last rather than against your own
# starting point.
.sentrux/
# E2E run artifacts (logs, SLURM output)
/log_artifacts/
